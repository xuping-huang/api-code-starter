{{#projectStyle.db.isDynamoDB}}
# Create the ResourceRole table
aws dynamodb create-table --table-name ResourceRole --attribute-definitions AttributeName=id,AttributeType=S --key-schema AttributeName=id,KeyType=HASH --provisioned-throughput ReadCapacityUnits=100,WriteCapacityUnits=100 --region ap-southeast-1 --endpoint-url http://dynamodb:8000

# Create the Resource table
aws dynamodb create-table --table-name Resource --attribute-definitions AttributeName=id,AttributeType=S AttributeName=challengeId,AttributeType=S --key-schema AttributeName=challengeId,KeyType=HASH AttributeName=id,KeyType=RANGE --provisioned-throughput ReadCapacityUnits=100,WriteCapacityUnits=100 --region ap-southeast-1 --endpoint-url http://dynamodb:8000

{{/projectStyle.db.isDynamoDB}}
tail -f /dev/null
